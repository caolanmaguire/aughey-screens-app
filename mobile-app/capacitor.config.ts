import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.augheyapp.app',
  appName: 'Aughey Screens Sales App',
  webDir: 'build'
};

export default config;
